# BibleRogue2 audio assets

Place the main background music file here.

Recommended files:

- `bgm-main.mp3`
- `bgm-main.ogg` optional fallback

The game remains playable even if these files are missing. BGM starts only after the user turns it on from Home > Settings.
